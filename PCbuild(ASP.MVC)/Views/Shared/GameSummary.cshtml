@model PCbuilder_ASP.MVC_.Models.ViewModel.GameViewModel

<div class="item">
    <h3>@Model.Name</h3>
    @if (@Model.ImageData64 != null || @Model.ImageData32 != null)
    {
        <img src="@Url.Action("GetImage", "Build", new { Model.GameGuid})" />
    }
</div>