@model PCbuild_ASP.MVC_.Models.Change

<td>
    @Html.DropDownListFor(x => x.CPUs, new SelectList(Model.CPUs.
AsEnumerable(), "CPUID", "ProcessorNumber"))
</td>
<td>
    @Html.DropDownListFor(x => x.GPUs, new SelectList(Model.GPUs.
AsEnumerable(), "GPUID", "Name"))
</td>